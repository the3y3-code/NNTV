/**
 * Global Configuration for the Frontend Application.
 * Centralizes API endpoints to make switching between Local/Dev/Prod environments deeper easier.
 */
const config = {
    // The base URL for the Python Flask Backend
    API_URL: 'http://localhost:5000'
};

export default config;
